import { css } from "@SMLCorp/styled-system/css";

const Browse = (): JSX.Element => (
  <>
    <div className={css({ fontSize: "2xl", fontWeight: "bold" })}>browse</div>
  </>
);

export default Browse;
